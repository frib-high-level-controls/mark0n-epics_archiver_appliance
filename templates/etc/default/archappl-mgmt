ARCHAPPL_SHORT_TERM_FOLDER=<%= @short_term_storage %>/ArchiverStore
ARCHAPPL_MEDIUM_TERM_FOLDER=<%= @mid_term_storage %>/ArchiverStore
ARCHAPPL_LONG_TERM_FOLDER=<%= @long_term_storage %>/ArchiverStore

ARCHAPPL_MYIDENTITY=appliance<%= @nodes_fqdn.index(@fqdn) %>
ARCHAPPL_APPLIANCES=/etc/archappl/appliances.xml
ARCHAPPL_POLICIES=<%= @real_policies_file %>
ARCHAPPL_PROPERTIES_FILENAME=<%= @real_properties_file %>

JAVA_OPTS="-XX:MaxPermSize=128M -Xmx128M -Xms128M -ea"

# Directory where the Tomcat 7 binary distribution resides
CATALINA_HOME=/usr/share/tomcat7

# Directory for per-instance configuration files and webapps
CATALINA_BASE=/var/lib/tomcat7-archappl/mgmt

EPICS_CA_AUTO_ADDR_LIST=yes

# Make sure address list is not empty. This is needed to work around a CAJ bug
# (see http://www.aps.anl.gov/epics/tech-talk/2014/msg00045.php).
EPICS_CA_ADDR_LIST=127.0.0.1